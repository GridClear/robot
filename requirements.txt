# Python deps for model generation, simulation, control, and training.
# Firmware uses PlatformIO (firmware/platformio.ini) — installed via this list too.
#
#   python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt

# model + sim
mujoco==3.9.0
numpy==2.4.6
PyYAML==6.0.3
pillow==12.2.0

# control (CLI, transports, mock server)
requests==2.34.2
websockets==16.0
aiohttp==3.13.5
bleak==3.0.2

# training + policy export/deploy (MuJoCo fallback path; Isaac Lab lives on the GB10)
gymnasium==1.2.3
stable-baselines3==2.8.0
torch==2.12.0
onnx==1.21.0
onnxruntime==1.26.0
onnxscript==0.7.0

# firmware toolchain
platformio==6.1.19
